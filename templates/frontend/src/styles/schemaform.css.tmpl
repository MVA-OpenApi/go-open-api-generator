h1 {
  text-align: center;
}

.schema-container {
  display: flex;
  flex-direction: column;
}

.schema-form {
  display: flex;
  flex-direction: column;
  margin-left: 1em;
  margin-right: 1em;
  margin-bottom: 1em;
  gap: 2em;
  padding-top: 1.35em;
}

form button {
  align-self: flex-end;
}
